# Sentinel Edge frontend

React, TypeScript, and Vite frontend for Sentinel Edge, the KSP crime-intelligence assistant.

## Run locally

```bash
npm install
npm run dev
```

Use `npm run build` to type-check and create a production build, and `npm run lint` to run static checks.
